services:
  - type: web
    name: parlare-webhook
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn whatsapp_webhook:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
