// MiniCalendar.js – Componentización del mini‑calendario

import { getStartOfWeek, addDays, formatDateLocal } from "../../utils/dateUtils.js";

export function initMiniCalendar() {
    console.log("🚀 initMiniCalendar – placeholder implementation");
    // Aquí iría la lógica del mini‑calendario (navegación de semanas, renderizado).
    // Por ahora, mantenemos la funcionalidad original en calendar.js para no romper la app.
}
